var JsScroll=(function(){let events={};function visible(el){let viewTop=window.pageYOffset;let viewBottom=viewTop+window.innerHeight;let rect=el.getBoundingClientRect();let _top=viewTop+rect.y;let _bottom=_top+rect.height;return(_top<=viewBottom)&&(_bottom>=viewTop);}
function callback(el,fire){el.classList.add('active');if(fire){return events[fire].call(el);}
return true;}
function getEvent(el){for(let fire in events){if(el.classList.contains(fire)){return fire;}}}
return{addEvent:function(name,fn){if(typeof fn=='function'){events[name]=fn;}},start:function(){let all=document.querySelectorAll('.on-scroll');if(all){all.forEach(function(el){let fire=getEvent(el);function fn(){if(visible(el)){if(callback(el,fire)){window.removeEventListener('scroll',fn);}}}
window.addEventListener('scroll',fn);fn();});}}};})();window.addEventListener('load',function(){JsScroll.start();});JsScroll.addEvent('fx-bg-py20',function(){let viewTop=window.pageYOffset;let rect=this.getBoundingClientRect();let _top=viewTop+rect.y;this.style.backgroundPositionY=((_top-viewTop)*.2)+'px';});JsScroll.addEvent('counter',function(){let all=this.querySelectorAll('[data-counter]');if(all){all.forEach(function(el){let top=el.getAttribute('data-counter');let num=0;let duration=1200;let steps=60;let increment=top/steps;let handle=setInterval(function(){if(num>=top){el.innerHTML=top;clearInterval(handle);}else{num+=increment;el.innerHTML=parseInt(num);}},duration/steps);});}
return true;});